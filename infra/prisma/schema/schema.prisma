generator client {
  provider = "prisma-client-js"
  output   = "../../../.prisma/client"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}
